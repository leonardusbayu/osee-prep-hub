import 'package:flutter/material.dart';

class VooResponsiveRow extends StatelessWidget {
  const VooResponsiveRow({super.key});

  @override
  Widget build(BuildContext context) => const Placeholder();
}