import 'package:flutter/material.dart';

class VooResponsiveColumn extends StatelessWidget {
  const VooResponsiveColumn({super.key});

  @override
  Widget build(BuildContext context) => const Placeholder();
}